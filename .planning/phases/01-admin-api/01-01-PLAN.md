---
phase: 01-admin-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/database.py
  - app/router/admin.py
  - app/main.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - API-01
  - API-02

must_haves:
  truths:
    - "管理员可以通过 POST /api/admin/auth/login 用用户名密码登录，获得 HttpOnly Cookie"
    - "首次使用（无用户时）可以通过 POST /api/admin/auth/register 创建管理员并自动登录"
    - "有用户后 POST /api/admin/auth/register 返回 403"
    - "未认证请求访问受保护端点返回 401"
    - "已认证请求在 7 天内 Cookie 有效"
    - "管理员可以修改密码和退出登录"
  artifacts:
    - path: "app/router/admin.py"
      provides: "Admin API 路由（认证端点 + 依赖注入认证）"
      exports: ["admin_router"]
    - path: "app/database.py"
      provides: "users 表 + 认证函数（hash_password, verify_password, create_user, get_user, update_user_password, has_users）+ WAL 模式"
      contains: "CREATE TABLE IF NOT EXISTS users"
  key_links:
    - from: "app/router/admin.py"
      to: "app/database.py"
      via: "from app.database import get_user, create_user, ..."
      pattern: "from app\\.database import"
    - from: "app/main.py"
      to: "app/router/admin.py"
      via: "app.include_router(admin_router)"
      pattern: "include_router.*admin"
---

<objective>
Admin API 路由骨架 + 完整认证端点

Purpose: 为 React 前端提供管理员认证 API（登录/注册/退出/改密），在 app/database.py 中补充 users 表和认证函数，在 app/main.py 中注册 admin 路由。
Output: app/router/admin.py（新文件）、app/database.py（修改）、app/main.py（修改）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-api/01-RESEARCH.md

<interfaces>
<!-- app/database.py 中 Admin API 需要复用的现有函数签名 -->

From app/database.py:
```python
def get_db_connection() -> sqlite3.Connection  # 已有，返回 sqlite3.Row row_factory
def get_db() -> contextmanager  # 已有，自动 commit/rollback/close
def init_database()  # 已有，需添加 users 表创建
def get_all_models() -> List[dict]
def create_model(model_data: dict) -> str
def update_model(config_id: str, model_data: dict)
def delete_model(config_id: str)
def get_all_api_keys() -> List[dict]
def create_api_key(name: str) -> dict
def delete_api_key(key_id: str)
def get_call_logs(limit: int = 100, offset: int = 0) -> List[dict]
def get_daily_stats() -> List[dict]
def get_auto_switch_status() -> bool
def set_auto_switch_status(enabled: bool)
```

From app/main.py:
```python
app = FastAPI(title="AI Proxy API", lifespan=lifespan)
# 现有路由: chat_router, responses_router
# CORS: allow_origins=["*"], allow_credentials=True
```

From streamlit_app/db.py（需迁移的认证函数）:
```python
def hash_password(password, salt=None) -> (hash_hex, salt)
def verify_password(password, password_hash, salt) -> bool
def create_user(username, password) -> bool
def get_user(username) -> Optional[dict]
def update_user_password(username, new_password) -> bool
def has_users() -> bool
```

From streamlit_app/home.py（需复制的 token 逻辑）:
```python
SECRET_KEY = "union_ai_secret"
def generate_auth_token(username) -> str  # base64(username:timestamp:sha256_hash[:16])
def verify_auth_token(token_b64) -> Optional[str]  # 验证签名+7天过期+用户存在
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 app/database.py 中添加 users 表 + 认证函数 + WAL 模式</name>
  <files>app/database.py</files>
  <read_first>
    - app/database.py（当前完整内容，了解 init_database 结构和 get_db_connection）
    - streamlit_app/db.py（参考 hash_password, verify_password, create_user, get_user, update_user_password, has_users 实现）
  </read_first>
  <action>
在 app/database.py 中做以下修改（所有新增代码放在 init_database() 之后、业务函数之前）：

1. 在 get_db_connection() 函数体中，`conn.row_factory = sqlite3.Row` 之后添加：
   ```python
   conn.execute("PRAGMA journal_mode=WAL")
   ```

2. 在 init_database() 函数体中，`conn.commit()` 之前添加 users 表创建：
   ```python
   cursor.execute("""
       CREATE TABLE IF NOT EXISTS users (
           id INTEGER PRIMARY KEY AUTOINCREMENT,
           username TEXT UNIQUE NOT NULL,
           password_hash TEXT NOT NULL,
           salt TEXT NOT NULL,
           created_at TEXT,
           updated_at TEXT
       )
   """)
   ```

3. 在 init_database() 之后添加以下认证函数（从 streamlit_app/db.py 迁移，改用 get_db() context manager 和 BEIJING_TZ 时间戳）：
   ```python
   def hash_password(password: str, salt: str = None) -> tuple:
       if salt is None:
           salt = secrets.token_hex(16)
       pwdhash = hashlib.pbkdf2_hmac('sha256', password.encode('utf-8'), salt.encode('utf-8'), 100000)
       return pwdhash.hex(), salt

   def verify_password(password: str, password_hash: str, salt: str) -> bool:
       pwdhash, _ = hash_password(password, salt)
       return pwdhash == password_hash

   def create_user(username: str, password: str) -> bool:
       password_hash, salt = hash_password(password)
       created_at = datetime.now(BEIJING_TZ).strftime('%Y-%m-%d %H:%M:%S')
       with get_db() as conn:
           cursor = conn.cursor()
           try:
               cursor.execute(
                   "INSERT INTO users (username, password_hash, salt, created_at, updated_at) VALUES (?, ?, ?, ?, ?)",
                   (username, password_hash, salt, created_at, created_at)
               )
               return True
           except sqlite3.IntegrityError:
               return False

   def get_user(username: str) -> Optional[dict]:
       with get_db() as conn:
           cursor = conn.cursor()
           cursor.execute("SELECT * FROM users WHERE username = ?", (username,))
           row = cursor.fetchone()
           return dict(row) if row else None

   def update_user_password(username: str, new_password: str) -> bool:
       password_hash, salt = hash_password(new_password)
       updated_at = datetime.now(BEIJING_TZ).strftime('%Y-%m-%d %H:%M:%S')
       with get_db() as conn:
           cursor = conn.cursor()
           cursor.execute(
               "UPDATE users SET password_hash = ?, salt = ?, updated_at = ? WHERE username = ?",
               (password_hash, salt, updated_at, username)
           )
           return True

   def has_users() -> bool:
       with get_db() as conn:
           cursor = conn.cursor()
           cursor.execute("SELECT COUNT(*) as count FROM users")
           row = cursor.fetchone()
           return row['count'] > 0
   ```

注意：需要在文件顶部 import hashlib（已有）和 secrets（需添加）。
  </action>
  <acceptance_criteria>
    - `app/database.py` 包含 `conn.execute("PRAGMA journal_mode=WAL")`
    - `app/database.py` 包含 `CREATE TABLE IF NOT EXISTS users`
    - `app/database.py` 包含 `def hash_password(password` 函数
    - `app/database.py` 包含 `def verify_password(password` 函数
    - `app/database.py` 包含 `def create_user(username` 函数
    - `app/database.py` 包含 `def get_user(username` 函数
    - `app/database.py` 包含 `def update_user_password(username` 函数
    - `app/database.py` 包含 `def has_users()` 函数
    - `import secrets` 在文件顶部
    - 所有新函数使用 `with get_db()` context manager（不是 streamlit_app/db.py 的 `get_db_connection()` 手动开关模式）
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/Jimmy/Softwares/union-ai-api" && python -c "from app.database import hash_password, verify_password, create_user, get_user, update_user_password, has_users; h, s = hash_password('test'); assert verify_password('test', h, s); print('auth functions OK')"</automated>
  </verify>
  <done>app/database.py 包含 users 表创建、6 个认证函数、WAL 模式，所有函数可正常导入</done>
</task>

<task type="auto">
  <name>Task 2: 创建 app/router/admin.py 认证端点</name>
  <files>app/router/admin.py</files>
  <read_first>
    - app/database.py（Task 1 完成后的版本，确认可导入的函数签名）
    - app/main.py（了解路由注册模式和 CORS 配置）
    - streamlit_app/home.py 第 110-142 行（token 生成/验证逻辑参考）
  </read_first>
  <action>
创建新文件 app/router/admin.py，包含：

1. 导入：
```python
from fastapi import APIRouter, Depends, HTTPException, Response, Cookie, Form, status
from typing import Optional
import hashlib
import base64
from datetime import datetime, timedelta
from app.database import (
    get_user, create_user, verify_password, has_users,
    update_user_password, hash_password
)
```

2. 路由器定义和 SECRET_KEY：
```python
admin_router = APIRouter(prefix="/api/admin")

SECRET_KEY = "union_ai_secret"  # 与 Streamlit 现有逻辑保持一致
```

3. Session token 函数（从 streamlit_app/home.py 的 generate_auth_token / verify_auth_token 迁移）：
```python
def create_session_token(username: str) -> str:
    timestamp = str(int(datetime.now().timestamp()))
    token_data = f"{username}:{timestamp}"
    token_hash = hashlib.sha256(f"{token_data}:{SECRET_KEY}".encode()).hexdigest()[:16]
    auth_token = f"{token_data}:{token_hash}"
    return base64.b64encode(auth_token.encode()).decode()

def verify_session_token(token_b64: str) -> Optional[str]:
    try:
        auth_token = base64.b64decode(token_b64.encode()).decode()
        parts = auth_token.split(":")
        if len(parts) != 3:
            return None
        username, timestamp, token_hash = parts
        token_time = datetime.fromtimestamp(int(timestamp))
        if datetime.now() - token_time > timedelta(days=7):
            return None
        expected_hash = hashlib.sha256(f"{username}:{timestamp}:{SECRET_KEY}".encode()).hexdigest()[:16]
        if token_hash != expected_hash:
            return None
        user = get_user(username)
        return username if user else None
    except Exception:
        return None
```

4. 依赖注入认证函数：
```python
async def get_current_user(session_token: Optional[str] = Cookie(None, alias="session_token")):
    if not session_token:
        raise HTTPException(status_code=401, detail="未认证")
    username = verify_session_token(session_token)
    if not username:
        raise HTTPException(status_code=401, detail="会话已过期")
    user = get_user(username)
    if not user:
        raise HTTPException(status_code=401, detail="用户不存在")
    # 不返回敏感字段
    return {"username": user["username"], "created_at": user.get("created_at")}
```

5. Cookie 设置辅助函数：
```python
COOKIE_MAX_AGE = 7 * 24 * 3600  # 7天

def set_auth_cookie(response: Response, token: str):
    response.set_cookie(
        key="session_token",
        value=token,
        httponly=True,
        max_age=COOKIE_MAX_AGE,
        samesite="lax",
        path="/"
    )
```

6. 认证端点：
- POST /api/admin/auth/register — 接受 Form(username, password)。先检查 `has_users()`，有用户则返回 403。验证用户名和密码非空，密码 >= 4 位。调用 `create_user()`，成功后设置 Cookie 并返回 `{"message": "注册成功", "username": username}`
- POST /api/admin/auth/login — 接受 Form(username, password)。调用 `get_user(username)` 获取用户，用 `verify_password()` 验证。失败返回 401。成功设置 Cookie 并返回 `{"message": "登录成功", "username": username}`
- POST /api/admin/auth/logout — 需要认证 (Depends(get_current_user))。`response.delete_cookie(key="session_token", path="/")`，返回 `{"message": "已退出登录"}`
- GET /api/admin/auth/me — 需要认证。返回 `{"username": current_user["username"]}`
- POST /api/admin/auth/change-password — 需要认证。接受 Form(old_password, new_password)。验证旧密码，新密码 >= 4 位，调用 `update_user_password()`
  </action>
  <acceptance_criteria>
    - `app/router/admin.py` 存在且包含 `admin_router = APIRouter(prefix="/api/admin")`
    - 包含 `async def get_current_user` 函数，使用 `Cookie(None, alias="session_token")`
    - 包含 `POST /auth/register` 端点，检查 `has_users()` 后决定是否允许注册
    - 包含 `POST /auth/login` 端点，使用 `verify_password()` 验证
    - 包含 `POST /auth/logout` 端点，使用 `response.delete_cookie()`
    - 包含 `GET /auth/me` 端点，使用 `Depends(get_current_user)`
    - 包含 `POST /auth/change-password` 端点，验证旧密码后更新
    - 所有 Cookie 设置使用 `httponly=True, samesite="lax", max_age=7*24*3600, path="/"`
    - `verify_session_token` 检查 7 天过期
    - get_current_user 返回值不包含 password_hash 和 salt 字段
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/Jimmy/Softwares/union-ai-api" && python -c "from app.router.admin import admin_router; routes = [r.path for r in admin_router.routes]; print('Routes:', routes); assert '/auth/login' in routes; assert '/auth/register' in routes; assert '/auth/logout' in routes; assert '/auth/me' in routes; assert '/auth/change-password' in routes; print('admin routes OK')"</automated>
  </verify>
  <done>app/router/admin.py 包含完整的认证端点（login, register, logout, me, change-password），使用 HttpOnly Cookie 认证，get_current_user 依赖注入</done>
</task>

<task type="auto">
  <name>Task 3: 在 app/main.py 中注册 admin 路由</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py（当前路由注册顺序和 import 结构）
    - app/router/admin.py（确认 admin_router 可导入）
  </read_first>
  <action>
在 app/main.py 中做以下修改：

1. 在现有 import 区域添加：
```python
from app.router.admin import admin_router
```

2. 在 `app.include_router(responses_router)` 之后添加：
```python
app.include_router(admin_router)
```

注意：admin 路由必须在现有 API 路由（chat_router, responses_router）之后注册，因为它们有更高优先级的路径匹配（/v1/* 和 /responses）。admin 路由前缀是 /api/admin/*，不会冲突。
  </action>
  <acceptance_criteria>
    - `app/main.py` 包含 `from app.router.admin import admin_router`
    - `app/main.py` 包含 `app.include_router(admin_router)`
    - `app.include_router(admin_router)` 出现在 `app.include_router(responses_router)` 之后
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/Jimmy/Softwares/union-ai-api" && python -c "
from app.main import app
routes = [r.path for r in app.routes if hasattr(r, 'path')]
admin_routes = [r for r in routes if '/api/admin' in r]
print('Admin routes:', admin_routes)
assert len(admin_routes) > 0, 'No admin routes found'
print('main.py registration OK')
"</automated>
  </verify>
  <done>app/main.py 成功注册 admin_router，所有认证端点可通过 /api/admin/auth/* 访问</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /api/admin/auth/* | 未认证输入（用户名/密码），来自不可信来源 |
| Browser -> /api/admin/* (非 auth) | Cookie 认证，需验证 session_token 完整性 |
| FastAPI -> SQLite | 本地数据库，可信边界内 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | /auth/login | mitigate | 使用 pbkdf2_hmac 验证密码，不存储明文 |
| T-01-02 | Tampering | session_token Cookie | mitigate | sha256 签名验证，篡改会导致签名不匹配 |
| T-01-03 | Information Disclosure | /auth/me, /auth/login 响应 | mitigate | 响应不包含 password_hash/salt 字段 |
| T-01-04 | Elevation of Privilege | /auth/register | mitigate | has_users() 检查，有用户后返回 403 |
| T-01-05 | Information Disclosure | session_token | mitigate | HttpOnly=True 防止 JS 读取 |
| T-01-06 | Tampering | 跨站请求 | mitigate | SameSite=Lax 防止跨站 POST 携带 Cookie |
| T-01-07 | Denial of Service | /auth/login | accept | 单用户管理面板，暴力破解风险低 |
</threat_model>

<verification>
```bash
# 1. 认证函数单元验证
cd "D:/Jimmy/Softwares/union-ai-api"
python -c "from app.database import hash_password, verify_password, create_user, get_user, has_users; h, s = hash_password('test123'); assert verify_password('test123', h, s); print('password hash OK')"

# 2. admin 路由注册验证
python -c "from app.main import app; routes = [r.path for r in app.routes if hasattr(r, 'path')]; print([r for r in routes if 'admin' in r])"

# 3. 快速启动测试（验证无 import 错误）
python -c "from app.main import app; print('FastAPI app loaded OK')"
```
</verification>

<success_criteria>
1. app/database.py 包含 users 表创建和 6 个认证函数，所有函数可正常导入
2. app/router/admin.py 包含 5 个认证端点（login, register, logout, me, change-password）
3. app/main.py 注册了 admin_router，所有 /api/admin/auth/* 端点可访问
4. Cookie 使用 httponly=True, samesite="lax", max_age=7天
5. has_users() 检查使 register 端点在有用户后返回 403
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-api/01-01-SUMMARY.md`
</output>
